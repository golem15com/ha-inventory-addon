# Test-only dependencies for the whereiput.it Inventory HA custom integration.
#
# pytest-homeassistant-custom-component must match the targeted HA core
# (hacs.json pins 2026.6.0). 0.13.339 ships Home Assistant 2026.6.x.
# CI installs this pin; if it cannot resolve locally, leave it floating and
# rely on CI for the matching pin.
pytest
pytest-homeassistant-custom-component==0.13.339
pytest-asyncio
aioresponses
# The manifest declares dependencies: ["conversation"]; setting that component
# up during tests (config-flow entry creation, __init__ setup) needs hassil +
# the bundled intents package, which PHACC does not pull in transitively.
hassil
home-assistant-intents
